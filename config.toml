baseurl = "https://www.freeanalystnotes.com//"
title = "Free Analyst Notes"
languageCode = "en-us"
paginate = "6" # Number of posts per page
theme = "Mainroad"
disqusShortname = "" # Enable comments by entering your Disqus shortname
googleAnalytics = "" # Enable Google Analytics by entering your tracking id

[Author]
  name = "Aaron Hardy"
  bio = "John Doe's true identity is unknown. Maybe he is a successful blogger or writer. Nobody knows it."
  avatar = "img/avatar.png"

[Params]
  description = "Study for the CFA exams with our free notes" # Description of your site
  opengraph = true
  twitter_cards = false
  readmore = false # Show "Read more" button in list if true
  authorbox = false
  pager = false
  post_meta = ["date", "categories"] # Order of post meta information
  mathjax = true
  mathjaxPath = "https://cdn.mathjax.org/mathjax/latest/MathJax.js"
  mathjaxConfig = "TeX-AMS-MML_HTMLorMML"
  toc = true
  summary = true
  post_navigation = true
  postSections = ["blog"]
#[Permalinks]
 # post = "/post/:slug/"
  
[Params.logo]
  image = "" # "img/placeholder.png"
  subtitle = "CFA Study Notes" # Logo subtitle

[Params.sidebar]
  home = "right"   # Configure layout for home page
  list = "right"   # Configure layout for list pages
  single = "right" # Configure layout for single pages
  # Enable widgets in given order
  widgets = ["categories", "taglist", "recent"] #"search"

[Params.widgets]
  recent_num = 5 # Set the number of articles in the "Recent articles" widget
  tags_counter = false # Enable counter for each tag in "Tags" widget (disabled by default)
  # sidemenu = true
